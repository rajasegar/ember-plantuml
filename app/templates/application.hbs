{{page-title "Ember PlantUML"}}
<header>
  <h1>Ember PlantUML</h1>
  <a href="https://github.com/rajasegar/ember-plantuml">Github</a>
</header>
<div class="grid-col">
  <aside>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <details>
          <summary>Components ({{@model.components.length}})</summary>
        {{#each @model.components as |component|}}
          <li>
            <LinkTo @route="uml" @query={{hash folder="app/components" file=component}}>{{component}}</LinkTo>
          </li>
        {{/each}}
        </details>
        <details>
          <summary>Models ({{@model.models.length}})</summary>
        {{#each @model.models as |model|}}
          <li>
            <LinkTo @route="uml" @query={{hash folder="app/models" file=model}}>{{model}}</LinkTo>
          </li>
        {{/each}}
        </details>
      </ul>
    </nav>
  </aside>
  <main>
    {{outlet}}
  </main>
</div>
